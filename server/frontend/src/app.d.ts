// See https://svelte.dev/docs/kit/types#app.d.ts for the reference on these
// interfaces. They are intentionally empty in the scaffold; the policy/auth
// surfaces fill them in from Phase 2 onwards.
declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}

export {};
