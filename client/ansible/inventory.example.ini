# Example inventory shape the dashboard runner generates at run time (see
# server/src/transport/ansible/inventory.ts). Used only for local
# syntax-check / lint; real runs get a per-run dynamic inventory.
[supervised]
mint-client.example.lan ansible_user=pct-agent
